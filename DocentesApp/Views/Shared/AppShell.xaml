<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="DocentesApp.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:DocentesApp" 
    Shell.FlyoutBehavior="Disabled">

    <Shell.ToolbarItems>
        <ToolbarItem Text="Cerrar Sesion" Order="Secondary" Command="{Binding CerrarSesionCommand}"></ToolbarItem>
    </Shell.ToolbarItems>
   
    
    <ShellContent
        Content="{Binding Vista}" />
    
    

</Shell>
